# tinker
function tinker () {
  if [ -z "$1" ]; then
    php artisan tinker
  else
    php artisan tinker --execute="dd($1);"
  fi
}

# redate last commit
function redate () {
  GIT_COMMITTER_DATE="$1" git commit --amend --no-edit --date $1
}
